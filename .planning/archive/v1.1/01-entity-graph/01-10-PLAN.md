---
phase: 01-entity-graph
plan: 10
type: execute
wave: 3
depends_on: [08]
files_modified:
  - folio_api/static/js/entity_graph.js
autonomous: true
requirements: [GRAPH-13, GRAPH-14]
tags: [frontend, pan-zoom, transform, fit-to-viewport]

must_haves:
  truths:
    - "Mouse-drag on the graph viewport translates the graph (pan)"
    - "Mouse-wheel scroll over the graph zooms in/out around the cursor position; zoom clamped to 0.2..4"
    - "On initial render after refreshFor resolves, fitGraph() centers the laid-out bounds inside the viewport (auto-fit)"
    - "After expand() (Plan 11) renders new nodes, fitGraph is NOT re-called; user's zoom/pan preserved"
    - "applyTransform() writes state.transform values to #graph-transform style.transform on every change"
  artifacts:
    - path: "folio_api/static/js/entity_graph.js"
      provides: "_panMouseDown/_panMouseMove/_panMouseUp + _onWheel + applyTransform + fitGraph + wirePanZoom"
      contains: ["fitGraph", "applyTransform", "wirePanZoom", "_onWheel"]
---

<objective>
Add the pan/zoom transform tracking ported from donor folio-enrich:9036-9118 plus an auto-fit-to-viewport function. This closes GRAPH-13 (drag-pan + scroll-zoom per D-16) and the auto-fit half of GRAPH-14 (UI-SPEC §Interaction Contract: "render graph with auto-zoom-to-fit"). After this plan, users can drag and zoom the rendered graph; the initial view auto-fits.

Per CONTEXT.md D-26: re-fit is NOT called after `+N Children` expansion (preserve user's zoom). Plan 11 will pass `{preserveZoom: true}` to skip the fit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-entity-graph/CONTEXT.md
@.planning/phases/01-entity-graph/01-RESEARCH.md
@.planning/phases/01-entity-graph/01-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add pan/zoom + applyTransform + fitGraph helpers</name>
  <files>folio_api/static/js/entity_graph.js</files>
  <read_first>
    - folio_api/static/js/entity_graph.js (current state with renderGraph from Plans 08/09)
    - .planning/phases/01-entity-graph/01-RESEARCH.md (lines 705-732 — pan/zoom transform port from donor 9054-9118)
    - .planning/phases/01-entity-graph/01-UI-SPEC.md (lines 218-228 — interaction contract for pan/zoom; min/max zoom 0.2..4)
  </read_first>
  <action>
Add five functions inside the IIFE in entity_graph.js:

(a) `applyTransform()` — writes state.transform to #graph-transform's style.transform:

    function applyTransform() {
        var el = document.getElementById('graph-transform');
        if (!el) return;
        var t = state.transform;
        el.style.transform = 'translate(' + t.x + 'px, ' + t.y + 'px) scale(' + t.scale + ')';
    }

(b) `fitGraph()` — computes the layout bounds and sets state.transform so the graph fits the viewport with 16 px padding on each side. After computing, calls applyTransform().

Algorithm:
- Read viewport rect via getBoundingClientRect() of #graph-viewport.
- If clientWidth or clientHeight is 0, requestAnimationFrame(fitGraph) and return (Pitfall 6).
- Read graph bounds: gw = max(child.x + child.width); gh = max(child.y + child.height) from state.graphData.layout.children.
- pad = 16; available = vw - 2*pad and vh - 2*pad.
- scale = min(available_w / gw, available_h / gh, 1) — never scale up beyond 1.
- scale = max(scale, 0.2) — respect min zoom.
- center: tx = (vw - gw * scale) / 2; ty = (vh - gh * scale) / 2.
- state.transform = { x: tx, y: ty, scale: scale }; applyTransform().

(c) `_onWheel(e)` — direct port of donor pattern (RESEARCH.md lines 712-725):

    function _onWheel(e) {
        e.preventDefault();
        var vp = document.getElementById('graph-viewport');
        if (!vp) return;
        var rect = vp.getBoundingClientRect();
        var mx = e.clientX - rect.left;
        var my = e.clientY - rect.top;
        var oldScale = state.transform.scale;
        var zoomFactor = e.deltaY < 0 ? 1.12 : 1 / 1.12;
        var newScale = Math.max(0.2, Math.min(4.0, oldScale * zoomFactor));
        state.transform.x = mx - (mx - state.transform.x) * (newScale / oldScale);
        state.transform.y = my - (my - state.transform.y) * (newScale / oldScale);
        state.transform.scale = newScale;
        applyTransform();
    }

(d) Pan handlers — `_panMouseDown`, `_panMouseMove`, `_panMouseUp`:

    var _panning = false;
    var _panStart = { x: 0, y: 0 };
    var _panStartXform = { x: 0, y: 0 };

    function _panMouseDown(e) {
        if (e.button !== 0) return;            // left button only
        if (e.target.closest('.graph-node')) return;  // node clicks handled in Plan 12
        _panning = true;
        _panStart = { x: e.clientX, y: e.clientY };
        _panStartXform = { x: state.transform.x, y: state.transform.y };
        document.addEventListener('mousemove', _panMouseMove);
        document.addEventListener('mouseup', _panMouseUp);
    }
    function _panMouseMove(e) {
        if (!_panning) return;
        state.transform.x = _panStartXform.x + (e.clientX - _panStart.x);
        state.transform.y = _panStartXform.y + (e.clientY - _panStart.y);
        applyTransform();
    }
    function _panMouseUp() {
        _panning = false;
        document.removeEventListener('mousemove', _panMouseMove);
        document.removeEventListener('mouseup', _panMouseUp);
    }

(e) `wirePanZoom()` — attaches the listeners to #graph-viewport once after renderGraph mounts the scaffold:

    function wirePanZoom() {
        var vp = document.getElementById('graph-viewport');
        if (!vp || vp.dataset.panZoomWired === '1') return;
        vp.dataset.panZoomWired = '1';
        vp.addEventListener('wheel', _onWheel, { passive: false });
        vp.addEventListener('mousedown', _panMouseDown);
    }

Call `wirePanZoom()` and `fitGraph()` from inside `_mountGraph` (Plan 08), AFTER the DOM has been appended to #entity-graph-pane. Wrap the fitGraph call in `requestAnimationFrame` to ensure the viewport has its real dimensions (Pitfall 6).

Add `applyTransform`, `fitGraph` to the public API export so Plan 11/13 can re-call applyTransform after layout merges.
  </action>
  <verify>
    <automated>node --check folio_api/static/js/entity_graph.js &amp;&amp; grep -q "function fitGraph" folio_api/static/js/entity_graph.js &amp;&amp; grep -q "function applyTransform" folio_api/static/js/entity_graph.js &amp;&amp; grep -q "wirePanZoom" folio_api/static/js/entity_graph.js &amp;&amp; grep -q "Math.max(0.2" folio_api/static/js/entity_graph.js &amp;&amp; grep -q "Math.min(4.0" folio_api/static/js/entity_graph.js</automated>
  </verify>
  <acceptance_criteria>
    - entity_graph.js parses with node --check exit 0
    - Defines fitGraph, applyTransform, wirePanZoom, _onWheel, _panMouseDown, _panMouseMove, _panMouseUp
    - Zoom clamped between 0.2 and 4.0 (verify both numbers literal in source)
    - wirePanZoom uses dataset.panZoomWired flag to avoid double-binding
    - Pan ignores clicks that originate inside .graph-node (node clicks handled in Plan 12)
    - Wheel handler calls preventDefault to suppress page scroll
    - Manual smoke: render graph, drag in empty area → graph translates; scroll wheel → zoom changes around cursor; reload → graph auto-fits viewport
  </acceptance_criteria>
  <done>Pan/zoom + auto-fit working; transform updates persist in state.transform.</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-W3-01 | DoS | Mousemove flood during pan | accept | One DOM write per mousemove; constant cost. Browsers throttle the event. |
| T-1-W3-02 | UX | Wheel preventDefault may surprise scroll users | accept | Standard for pan/zoom canvases per donor; UI-SPEC sanctions it. |
</threat_model>

<verification>
node --check folio_api/static/js/entity_graph.js
# Manual: drag canvas → pans; wheel → zooms; first render → auto-fit
</verification>

<success_criteria>
- Drag-pan works in empty viewport space
- Scroll-zoom clamped 0.2..4.0
- Auto-fit on initial render
- state.transform updated after every gesture
</success_criteria>

<output>
.planning/phases/01-entity-graph/01-10-SUMMARY.md should record:
- Manual UAT results for pan, zoom, fit
- Confirmed clamp values 0.2 and 4.0
</output>

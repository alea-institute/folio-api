---
phase: 01-entity-graph
plan: 05
type: execute
wave: 1
depends_on: [03]
files_modified:
  - folio_api/templates/jinja2/explore/tree.html
  - folio_api/static/js/unified_tree.js
autonomous: true
requirements: [GRAPH-05, GRAPH-11, GRAPH-12]
tags: [frontend, tabs, html-structure, custom-events]

must_haves:
  truths:
    - "User can click 'Details' or 'Entity Graph' tab buttons and see the corresponding panel become visible"
    - "Selecting an entity in the left tree dispatches an 'entity:selected' CustomEvent on document"
    - "Active tab carries the documented active styling; inactive tab shows the inactive styling"
    - "Tab strip exposes role='tablist' with role='tab'/role='tabpanel' children and proper aria-selected/aria-controls/aria-labelledby wiring"
    - "Existing #class-details fetch-and-render pipeline (loadDetails) is unchanged"
  artifacts:
    - path: "folio_api/templates/jinja2/explore/tree.html"
      provides: "Tab strip + tab panels DOM scaffolding inside #detail-container"
      contains:
        - 'id="detail-tabs"'
        - 'role="tablist"'
        - 'id="tab-details"'
        - 'id="tab-graph"'
        - 'id="tab-panel-details"'
        - 'id="tab-panel-graph"'
        - 'id="entity-graph-pane"'
    - path: "folio_api/static/js/unified_tree.js"
      provides: "Tab-switch handler + entity:selected dispatch"
      contains: ["entity:selected", "CustomEvent", "wireDetailTabs"]
  key_links:
    - from: "unified_tree.js#selectNode"
      to: "document 'entity:selected' event"
      via: "dispatchEvent(new CustomEvent('entity:selected', {detail: {iri, type}}))"
      pattern: "dispatchEvent\\(new CustomEvent"
    - from: "unified_tree.js#wireDetailTabs"
      to: "tab buttons in tree.html"
      via: "click handlers swap .hidden class and aria-selected attrs"
      pattern: "wireDetailTabs"
    - from: "tree.html#tab-graph"
      to: "EntityGraph.state.activeTab"
      via: "tab handler sets window.EntityGraph.state.activeTab = 'graph'"
      pattern: "EntityGraph\\.state"
---

<objective>
Add the Details / Entity Graph tab strip to the right detail pane on `/explore/tree` (per D-05/D-06, GRAPH-11) and wire the tab-switch handler in `unified_tree.js`. This plan also adds a dispatchEvent line to the existing `selectNode()` function — the decoupled event-based coupling pattern recommended in RESEARCH.md "Pattern 1" — so the entity-graph module (Plan 03 skeleton + later plans) can react to tree selection without `unified_tree.js` knowing about graphs.

Per UI-SPEC §Layout & Z-Index, the existing `#detail-container` is wrapped with:
- A new `#detail-tabs` element with role tablist containing two button tabs (Details default-active, Entity Graph)
- A new `#tab-panels` flex-grow container holding two role-tabpanel divs
- The Details panel wraps the EXISTING `#class-details` div byte-for-byte
- The Graph panel contains a fresh `#entity-graph-pane` element (empty in this plan)

Per D-06 ("graph replaces the entity card while Graph tab is active") the tab handler toggles the `.hidden` Tailwind class — never `display: none` directly, never destructive DOM moves, no jQuery UI tabs widget.

Purpose: Provides the structural DOM and the click-to-swap handler that all subsequent rendering plans (06, 07, 08, 09, 10, 11, 12, 13) hang from. Closes GRAPH-11. Wires GRAPH-05 (Plan 12 will check `state.activeTab === 'graph'` before fetching). Wires GRAPH-12 (Plan 12 will react to `entity:selected` dispatched here).

Output: ~25 lines of HTML inserted in `tree.html`; ~85 lines added to `unified_tree.js` (tab handler + 1-block dispatch in selectNode).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-entity-graph/CONTEXT.md
@.planning/phases/01-entity-graph/01-RESEARCH.md
@.planning/phases/01-entity-graph/01-UI-SPEC.md

<interfaces>
Existing tree.html structure (verified at planning time, lines 213-225):

  div#detail-container.w-full.md:w-3/5.lg:w-2/3.p-3.bg-gray-50...overflow-y-auto
    div#class-details
      (existing detail card content; populated by loadDetails() in unified_tree.js)

Existing unified_tree.js integration points (verified at planning time):
  Line 179: function selectNode(li, updateUrl) { ... loadDetails(...) ... }
  Line 253: function selectNodeByIri(iri) { ... }
  Line 295: window.selectNodeByIri = selectNodeByIri;

Tab strip target shape (per UI-SPEC §Layout & Z-Index, Detail-pane structural change):

  div#detail-container
    div#detail-tabs role=tablist aria-label="Detail view"
      button#tab-details role=tab aria-selected=true  aria-controls=tab-panel-details tabindex=0
      button#tab-graph   role=tab aria-selected=false aria-controls=tab-panel-graph   tabindex=-1
    div#tab-panels.flex-grow.overflow-hidden
      div#tab-panel-details role=tabpanel aria-labelledby=tab-details
        div#class-details (ORIGINAL CONTENT preserved)
      div#tab-panel-graph role=tabpanel aria-labelledby=tab-graph hidden
        div#entity-graph-pane (empty in this plan)

Active tab classes (UI-SPEC §State Inventory):
  Active:   bg-white text-blue-700 border-b-2 border-blue-600 px-4 py-2 text-sm font-semibold
  Inactive: bg-gray-100 text-gray-600 border-b-2 border-transparent hover:bg-gray-50 px-4 py-2 text-sm font-semibold
  Focus:    focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-blue-500 focus-visible:ring-offset-2
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wrap class-details with tab strip and tab panels in tree.html</name>
  <files>folio_api/templates/jinja2/explore/tree.html</files>
  <read_first>
    - folio_api/templates/jinja2/explore/tree.html (current state, focus on lines 213-225 — the `#detail-container` div and its single child `#class-details`)
    - .planning/phases/01-entity-graph/01-UI-SPEC.md (lines 154-186 — Layout & Z-Index; lines 286-291 — tab-strip state inventory with exact Tailwind classes)
    - .planning/phases/01-entity-graph/CONTEXT.md (D-05, D-06, D-11 — tab strip placement + ARIA tablist)
  </read_first>
  <action>
Edit `folio_api/templates/jinja2/explore/tree.html`. Locate the existing block that begins:

    div#detail-container... (around line 213)
      div#class-details
        ...existing content...
      /div
    /div

Wrap `#class-details` with the new tab strip + tab panels structure. The entire BLOCK INSIDE `#detail-container` must become the equivalent of the following (rendered HTML; use the Write tool to overwrite the whole file or carefully re-apply via the Bash sed-equivalent approach — but DO NOT damage other parts of tree.html):

Render the inside of `#detail-container` as:

1. A `<div id="detail-tabs" role="tablist" aria-label="Detail view" class="flex border-b border-gray-300 mb-3 -mx-3 px-3 bg-white sticky top-0 z-20">` containing two buttons:
   - `<button id="tab-details" role="tab" type="button" aria-selected="true" aria-controls="tab-panel-details" tabindex="0" class="px-4 py-2 text-sm font-semibold border-b-2 border-blue-600 bg-white text-blue-700 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-blue-500 focus-visible:ring-offset-2">Details</button>`
   - `<button id="tab-graph" role="tab" type="button" aria-selected="false" aria-controls="tab-panel-graph" tabindex="-1" class="px-4 py-2 text-sm font-semibold border-b-2 border-transparent bg-gray-100 text-gray-600 hover:bg-gray-50 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-blue-500 focus-visible:ring-offset-2">Entity Graph</button>`

2. A `<div id="tab-panels" class="flex-grow flex flex-col overflow-hidden">` containing two child panels:
   - `<div id="tab-panel-details" role="tabpanel" aria-labelledby="tab-details" class="flex-grow overflow-y-auto">` — INSIDE this div, place the ORIGINAL `<div id="class-details">...</div>` byte-for-byte (entire original content preserved verbatim).
   - `<div id="tab-panel-graph" role="tabpanel" aria-labelledby="tab-graph" class="flex-grow overflow-hidden hidden" hidden>` — inside this div place a single child `<div id="entity-graph-pane" class="w-full h-full bg-white relative"></div>` (empty; later plans fill it).

ARIA notes (per UI-SPEC §Interaction Contract):
- role tablist on the container; role tab on each button; role tabpanel on each panel.
- Active tab gets aria-selected true AND tabindex 0.
- Inactive tab gets aria-selected false AND tabindex -1 (Tab key skips it; arrow keys cycle within the tablist — Plan 12 wires arrow keys).
- aria-controls points from each tab to its panel id; aria-labelledby points from each panel back to its tab id.
- The graph panel uses BOTH `class="...hidden"` AND the HTML5 hidden attribute as a defensive default in case CSS hasn't loaded.

Do NOT:
- Add the jQuery UI tabs widget. Plain buttons + click handlers + .hidden toggling per D-05.
- Modify any other part of tree.html (search header, tree, scripts).
- Inline a tab-switch handler in tree.html — the handler goes in unified_tree.js per Task 2.
- Modify any text or HTML inside the original `#class-details` div. Existing `loadDetails()` JS in unified_tree.js does `getElementById('class-details').innerHTML = ...` — that contract is preserved by keeping the id on the same inner div.
  </action>
  <verify>
    <automated>grep -q 'id="detail-tabs"' folio_api/templates/jinja2/explore/tree.html &amp;&amp; grep -q 'id="tab-graph"' folio_api/templates/jinja2/explore/tree.html &amp;&amp; grep -q 'id="entity-graph-pane"' folio_api/templates/jinja2/explore/tree.html &amp;&amp; grep -q 'role="tablist"' folio_api/templates/jinja2/explore/tree.html &amp;&amp; grep -q 'id="class-details"' folio_api/templates/jinja2/explore/tree.html</automated>
  </verify>
  <acceptance_criteria>
    - tree.html contains exactly one of each id: `id="detail-tabs"`, `id="tab-details"`, `id="tab-graph"`, `id="tab-panel-details"`, `id="tab-panel-graph"`, `id="entity-graph-pane"`, `id="class-details"`
    - tree.html contains exactly one `role="tablist"` and exactly two `role="tabpanel"` strings
    - The Details tab carries `aria-selected="true"` and `tabindex="0"`; Graph tab carries `aria-selected="false"` and `tabindex="-1"`
    - The Graph panel carries both the Tailwind `hidden` utility class AND the bare `hidden` HTML attribute
    - Original content of `#class-details` is preserved (verify via `git diff` showing only insertions around it, no edits inside)
    - Loading `/explore/tree` in a browser still renders the existing detail card on first paint (Details tab is the active default)
  </acceptance_criteria>
  <done>Tab strip + tab panels DOM is in place; original detail content untouched; Graph panel hidden by default.</done>
</task>

<task type="auto">
  <name>Task 2: Add tab-switch handler and entity:selected dispatch in unified_tree.js</name>
  <files>folio_api/static/js/unified_tree.js</files>
  <read_first>
    - folio_api/static/js/unified_tree.js (focus on the `selectNode` function around line 179 — find the END of its body where we add the dispatch block; also find the END of file before `window.selectNodeByIri = selectNodeByIri;` at line 295, where we append the new wireDetailTabs and DOMContentLoaded boot)
    - .planning/phases/01-entity-graph/01-RESEARCH.md (lines 292-319 — Pattern 1 Custom-Event-Based Coupling; lines 440-444 — Pitfall 7 dedupe note relevant to event handler design)
    - .planning/phases/01-entity-graph/01-UI-SPEC.md (lines 235-245 — Keyboard navigation; arrow keys on tablist)
  </read_first>
  <action>
Make TWO edits to `folio_api/static/js/unified_tree.js`:

EDIT 1: dispatchEvent block at the END of `selectNode(li, updateUrl)` body (around line 179).

Find the closing brace `}` that terminates `function selectNode(li, updateUrl) { ... }` and insert the following block IMMEDIATELY before that closing brace (so the dispatch is the last action of selectNode):

    // Phase 1 (entity-graph): notify subscribers (entity_graph.js) that the
    // user picked a new entity. Custom event keeps unified_tree.js unaware
    // of the graph module (RESEARCH.md Pattern 1 — Custom-Event-Based Coupling).
    try {
        var _iri  = (typeof li !== 'undefined' && li && li.data) ? li.data('id') : null;
        var _type = (typeof li !== 'undefined' && li && li.data) ? li.data('type') : null;
        if (_iri) {
            document.dispatchEvent(new CustomEvent('entity:selected', {
                detail: { iri: _iri, type: _type }
            }));
        }
    } catch (e) {
        if (window.console && window.console.warn) {
            window.console.warn('entity:selected dispatch failed:', e);
        }
    }

The try/catch ensures legacy detail-card flow still works if `li` is unexpectedly malformed — graph dispatch is additive, not load-bearing.

EDIT 2: Append the tab-switch handler + boot at the END of the file, BEFORE the line `window.selectNodeByIri = selectNodeByIri;` (around line 295). Insert the following block:

    // ============================================================
    // Phase 1: Detail-pane tab strip (Details | Entity Graph)
    // Wires the buttons added by tree.html (Plan 05 Task 1) to swap
    // panel visibility. Updates window.EntityGraph.state.activeTab so
    // the graph module knows whether to fetch on entity:selected.
    // ============================================================

    function wireDetailTabs() {
        var tabDetails = document.getElementById('tab-details');
        var tabGraph   = document.getElementById('tab-graph');
        var panelDetails = document.getElementById('tab-panel-details');
        var panelGraph   = document.getElementById('tab-panel-graph');
        if (!tabDetails || !tabGraph || !panelDetails || !panelGraph) {
            // tree.html structure not present (e.g. dev page); nothing to wire.
            return;
        }

        var ACTIVE_CLS = 'px-4 py-2 text-sm font-semibold border-b-2 border-blue-600 bg-white text-blue-700 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-blue-500 focus-visible:ring-offset-2';
        var INACTIVE_CLS = 'px-4 py-2 text-sm font-semibold border-b-2 border-transparent bg-gray-100 text-gray-600 hover:bg-gray-50 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-blue-500 focus-visible:ring-offset-2';

        function activate(which) {
            var detailsActive = (which === 'details');
            tabDetails.setAttribute('aria-selected', detailsActive ? 'true' : 'false');
            tabGraph.setAttribute('aria-selected',   detailsActive ? 'false' : 'true');
            tabDetails.setAttribute('tabindex', detailsActive ? '0' : '-1');
            tabGraph.setAttribute('tabindex',   detailsActive ? '-1' : '0');
            tabDetails.className = detailsActive ? ACTIVE_CLS : INACTIVE_CLS;
            tabGraph.className   = detailsActive ? INACTIVE_CLS : ACTIVE_CLS;
            if (detailsActive) {
                panelDetails.classList.remove('hidden');
                panelDetails.removeAttribute('hidden');
                panelGraph.classList.add('hidden');
                panelGraph.setAttribute('hidden', '');
            } else {
                panelGraph.classList.remove('hidden');
                panelGraph.removeAttribute('hidden');
                panelDetails.classList.add('hidden');
                panelDetails.setAttribute('hidden', '');
            }
            if (window.EntityGraph && window.EntityGraph.state) {
                window.EntityGraph.state.activeTab = detailsActive ? 'details' : 'graph';
            }
            if (!detailsActive && window.EntityGraph && typeof window.EntityGraph.onTabActivated === 'function') {
                try { window.EntityGraph.onTabActivated(); } catch (err) {
                    if (window.console && window.console.error) window.console.error(err);
                }
            }
        }

        tabDetails.addEventListener('click', function () { activate('details'); });
        tabGraph.addEventListener('click',   function () { activate('graph');   });

        // Arrow-key cycling per UI-SPEC §Interaction Contract (ARIA tablist pattern).
        function onKey(e) {
            if (e.key !== 'ArrowLeft' && e.key !== 'ArrowRight') return;
            e.preventDefault();
            var current = (document.activeElement === tabGraph) ? 'graph' : 'details';
            var next = (current === 'details') ? 'graph' : 'details';
            activate(next);
            (next === 'graph' ? tabGraph : tabDetails).focus();
        }
        tabDetails.addEventListener('keydown', onKey);
        tabGraph.addEventListener('keydown',   onKey);
    }

    if (document.readyState === 'loading') {
        document.addEventListener('DOMContentLoaded', wireDetailTabs);
    } else {
        wireDetailTabs();
    }

Notes:
- The `window.EntityGraph.onTabActivated` hook is referenced here but implemented later (Plan 12). The defensive `typeof === 'function'` guard means this code is safe to run before that hook is defined.
- Arrow-key cycling matches the standard ARIA tablist pattern; only Left/Right are intercepted (Up/Down are reserved for tree navigation).
- The handler does NOT call `loadDetails()` on tab swap — switching to Details just shows the existing card unchanged (D-06 / UI-SPEC).
- Existing `window.selectNodeByIri = selectNodeByIri;` line stays untouched.

Do NOT:
- Modify any other existing function in unified_tree.js (selectNodeByIri, loadDetails, etc.).
- Move the dispatchEvent block to the top of selectNode — it MUST run after loadDetails has been kicked off, so subscribers see the same state the user does.
- Use jQuery's `.trigger()` — the listener side (Plan 03 entity_graph.js) uses native `addEventListener`, which only catches native events.
  </action>
  <verify>
    <automated>node --check folio_api/static/js/unified_tree.js &amp;&amp; grep -q "wireDetailTabs" folio_api/static/js/unified_tree.js &amp;&amp; grep -q "entity:selected" folio_api/static/js/unified_tree.js &amp;&amp; grep -q "CustomEvent" folio_api/static/js/unified_tree.js</automated>
  </verify>
  <acceptance_criteria>
    - unified_tree.js parses with no syntax errors (`node --check folio_api/static/js/unified_tree.js` exits 0)
    - Contains exactly one definition of `function wireDetailTabs` and exactly one DOMContentLoaded hook calling it
    - Contains the literal string `entity:selected` at least twice (one in the dispatch, one in a comment is acceptable)
    - Contains `dispatchEvent(new CustomEvent('entity:selected'` exactly once (the dispatch in selectNode)
    - The `wireDetailTabs` function references all four DOM ids: tab-details, tab-graph, tab-panel-details, tab-panel-graph
    - Existing `window.selectNodeByIri = selectNodeByIri;` line is preserved (not removed/relocated)
    - Manual smoke (run `uv run uvicorn folio_api.api:app`, open `/explore/tree`, open DevTools): clicking the Entity Graph tab swaps panels visibly; clicking back to Details restores the entity card
    - Selecting any tree entity logs no errors; with the EntityGraph module loaded, `window.EntityGraph.state.activeTab` reflects the current tab
  </acceptance_criteria>
  <done>Tab buttons swap panels with proper ARIA + visual states; selectNode dispatches entity:selected; arrow-key cycling works; existing tree behavior unaffected.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser DOM → unified_tree.js handlers | Click and keyboard events from a public user. No PII; no auth. |
| unified_tree.js → window.EntityGraph | Sets state.activeTab and conditionally calls onTabActivated. Both are public, intentional surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-W1-01 | Tampering | entity:selected event detail | accept | Public event with public ontology IRIs in detail. No PII; no privileged data. Subscribers must be defensive against malformed detail (RESEARCH.md Pitfall 7). |
| T-1-W1-02 | DoS | Arrow-key spam on tablist | accept | Tab swap is a DOM class toggle; cost is constant. No fetch fires from tab swap alone (D-10 lazy fetch). |
| T-1-W1-03 | Information Disclosure | Console warnings | accept | console.warn only logs string error messages, no IRIs or PII. |
</threat_model>

<verification>
Run after both tasks:

    node --check folio_api/static/js/unified_tree.js
    grep -q 'id="detail-tabs"' folio_api/templates/jinja2/explore/tree.html
    grep -q 'id="entity-graph-pane"' folio_api/templates/jinja2/explore/tree.html
    grep -c "dispatchEvent(new CustomEvent('entity:selected'" folio_api/static/js/unified_tree.js
    # Manual: uv run uvicorn folio_api.api:app, then open /explore/tree, click tabs, observe panel swap.
</verification>

<success_criteria>
- Tab strip renders inside #detail-container with the documented active/inactive Tailwind classes
- Clicking either tab swaps panel visibility and updates ARIA state
- Arrow Left/Right cycles between tabs (ARIA tablist pattern)
- Selecting a tree entity dispatches entity:selected once per click
- window.EntityGraph.state.activeTab mirrors the visible tab after every swap
- Existing detail-card behavior is unchanged (loadDetails still fires; #class-details still receives innerHTML)
- No JS console errors on /explore/tree load
</success_criteria>

<output>
After completion, create `.planning/phases/01-entity-graph/01-05-SUMMARY.md` documenting:
- Final tab-strip Tailwind classes (any deviation from UI-SPEC must be flagged)
- Confirmation that #class-details inner content is preserved (git diff inspection)
- Manual UAT result: tab swap visible? entity:selected dispatch verified in console (e.g. via document.addEventListener('entity:selected', console.log))?
</output>
